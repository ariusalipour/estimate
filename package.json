{
  "name": "estimate",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix worker install && npm --prefix worker run build",
    "dev": "npm --prefix frontend run dev",
    "deploy": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix worker install && npm --prefix worker run deploy"
  }
}
