{
  "name": "estimate",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../worker && npm run deploy",
    "dev": "cd frontend && npm run build && cd ../worker && npm run dev"
  }
}
