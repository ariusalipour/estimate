{
  "name": "estimate-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build-frontend": "cd ../frontend && npm run build",
    "dev:full": "cd ../frontend && npm run build && cd ../worker && wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.107.0"
  }
}
